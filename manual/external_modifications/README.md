# Manual External Modifications

These files represent the manual step-4 downstream edit described in the case study.

- `uc_v2_candidate.ssd` is the manually prepared candidate artifact set that is accepted for synchronization.

The raw change intent remains available as `inputs/uc_v2.ssd`. The workflow uses that raw file to demonstrate validation rejection before applying the manually prepared candidate edit. Any newly introduced parts are derived from the supplied candidate SSD during synchronization rather than from separate hand-maintained SysML definitions.
